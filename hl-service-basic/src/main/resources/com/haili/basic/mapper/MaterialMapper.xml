<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.haili.basic.mapper.MaterialMapper">
    <select id="getMaterialDtoList" resultType="com.haili.framework.domain.basic.dto.MaterialDto">
	    SELECT material.*, tb_material_type.name type_name
		FROM (SELECT * FROM tb_material ${ew.customSqlSegment}) material
        LEFT JOIN tb_material_type
        ON material.type_id=tb_material_type.id
    </select>
</mapper>